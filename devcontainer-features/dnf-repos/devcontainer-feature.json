{
  "id": "dnf-repos",
  "version": "0.1.0",
  "name": "DNF Repositories",
  "description": "Write one or more DNF/YUM repo files from declarative JSON definitions.",
  "options": {
    "REPOS_JSON": {
      "type": "string",
      "default": "[]",
      "description": "JSON array of repo definitions to write into /etc/yum.repos.d."
    }
  }
}

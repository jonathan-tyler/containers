{
  "id": "opencode-server",
  "version": "0.1.0",
  "name": "OpenCode Server",
  "documentationURL": "https://opencode.ai/docs/server/",
  "description": "Install OpenCode for headless HTTP server usage with `opencode serve`.",
  "options": {
    "VERSION": {
      "type": "string",
      "default": "latest",
      "description": "OpenCode version to install. Use 'latest' for the current release."
    }
  },
  "customizations": {
    "vscode": {
      "settings": {
        "github.copilot.chat.codeGeneration.instructions": [
          {
            "text": "This dev container includes OpenCode for headless server usage. Run `opencode serve` to expose the HTTP API, and use `OPENCODE_SERVER_PASSWORD` to protect it with basic auth."
          }
        ]
      }
    }
  }
}

## Notes

- This feature is the place for network, remote-access, and low-level diagnostic tooling.
- It is a better fit for human-operated or explicitly higher-trust workflows than unattended agent defaults.

## Limitations

- This feature currently targets Fedora and RHEL-family images with `dnf`.

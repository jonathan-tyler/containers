#!/usr/bin/env bash

set -euo pipefail

for arg in "$@"; do
    case "${arg}" in
        *raw.githubusercontent.com/Homebrew/install/HEAD/install.sh*)
            touch /tmp/homebrew-bootstrap-attempted
            echo "unexpected Homebrew bootstrap attempt" >&2
            exit 1
            ;;
    esac
done

exit 0

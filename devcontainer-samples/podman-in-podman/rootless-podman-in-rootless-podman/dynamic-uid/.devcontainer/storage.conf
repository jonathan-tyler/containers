# VFS avoids exposing /dev/fuse to the outer container. It is slower and uses
# more disk than overlay storage, but it preserves the narrow privilege model.
[storage]
driver = "vfs"
runroot = "/run/user/1000/containers"
graphroot = "/home/nonroot/.local/share/containers/storage"

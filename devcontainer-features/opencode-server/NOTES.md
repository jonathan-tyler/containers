## Notes

- This feature is focused on the documented `opencode serve` workflow and installs only the OpenCode binary.
- Authentication and runtime settings are intentionally left to environment variables and later config.

## Limitations

- This feature currently targets Fedora and RHEL-family images with `dnf`.
- The release archive naming is currently hardcoded for Linux `x64` and `arm64` builds.

{
  "id": "podman-in-podman",
  "version": "0.1.1",
  "name": "Podman (Podman-in-Podman)",
  "documentationURL": "https://www.redhat.com/en/blog/podman-inside-container",
  "description": "Run Podman inside the dev container with isolated storage volumes and container-friendly Podman defaults.",
  "installsAfter": [
    "nonroot-user",
    "homebrew-packages"
  ],
  "options": {
    "username": {
      "type": "string",
      "proposals": [
        "automatic",
        "nonroot",
        "devcontainer",
        "vscode",
        "node",
        "codespace",
        "podman",
        "ubuntu",
        "root"
      ],
      "default": "automatic",
      "description": "Preferred named non-root passwd user that should receive rootless subuid and subgid ranges when the image already defines one. Falls back to root when no named non-root user is available."
    }
  },
  "entrypoint": "/usr/local/share/podman-in-podman-init.sh",
  "containerEnv": {
    "_CONTAINERS_USERNS_CONFIGURED": "",
    "BUILDAH_ISOLATION": "chroot"
  },
  "securityOpt": [
    "label=disable"
  ],
  "mounts": [
    {
      "source": "pinp-var-lib-containers-${devcontainerId}",
      "target": "/var/lib/containers",
      "type": "volume"
    },
    {
      "source": "pinp-podman-home-${devcontainerId}",
      "target": "/home/podman/.local/share/containers",
      "type": "volume"
    }
  ]
}
